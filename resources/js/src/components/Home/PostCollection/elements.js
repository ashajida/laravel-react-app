import styled from 'styled-components';
import { Container as PostContainer, Wrapper as PostWrapper} from '../../extend';

export const Section = styled.section``;
export const Container = styled(PostContainer)``;
export const Wrapper = styled(PostWrapper)``;
export const PostsCollectionWrapper = styled.div`
    width: 100%;
    max-width: 600px;
    margin: 0 auto;
`;

